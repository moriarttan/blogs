<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blogs.mapper.ArticleMapper">

    <select id="pageList" resultType="com.example.blogs.vo.ArticleVO">
        select
        id, title, content, category_id, like_number, visit_number, size_number, `like`, comment, status, deleted, create_user_id, create_username, create_time, update_user_id, update_username, update_time
        from article
    </select>
</mapper>

