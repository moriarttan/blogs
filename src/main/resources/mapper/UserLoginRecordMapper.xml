<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blogs.mapper.UserLoginRecordMapper">

    <select id="pageList" resultType="com.example.blogs.vo.UserLoginRecordVO">
        select
        id, user_id, username, type, create_time
        from user_login_record
    </select>
</mapper>

